[package]
name = "dictator-macos-bridge"
version = "0.1.0"
edition = "2021"
authors = ["Implose Labs"]
license = "MIT"
description = "Minimal macOS-native bridge for Rust/Tauri Dictator parity"
repository = "https://github.com/michael-berardi/dictator"

[build-dependencies]

[dependencies]
libc = "0.2"

[lib]
crate-type = ["rlib"]
