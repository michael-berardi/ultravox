[package]
name = "ultravox-macos-bridge"
version = "0.3.2"
edition = "2021"
authors = ["Implose Labs"]
license = "MIT"
description = "Minimal macOS-native bridge for Rust/Tauri UltraVox parity"
repository = "https://github.com/michael-berardi/ultravox"

[build-dependencies]

[dependencies]
libc = "0.2"

[lib]
crate-type = ["rlib"]
